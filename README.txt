To create a virtual environment, add sample data to the database and launch the application run ./initialisation.sh

To only add sample data to the database, you have to activate the virtual environment and run ./create_database.sh

To only install the flask environment you have to activate the virtual environment and run ./instal_venv.sh

To delete the database just run ./delete_database.sh

database_test.py contains three functions to test the database
